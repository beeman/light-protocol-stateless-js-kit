{
  "dependencies": {
    "@codama/nodes-from-anchor": "^1.3.8",
    "@codama/renderers-js": "^1.7.0",
    "@solana/kit": "^6.0.1",
    "@solana/kit-plugin-rpc": "^0.3.0"
  },
  "devDependencies": {
    "@beeman/testcontainers": "^1.1.0",
    "@biomejs/biome": "^2.4.0",
    "@intellectronica/ruler": "^0.3.34",
    "@solana/kit-plugins": "^0.3.1",
    "@types/bun": "^1.3.9",
    "codama": "^1.5.0",
    "pkg-pr-new": "^0.0.62",
    "publint": "^0.3.16",
    "testcontainers": "^11.11.0",
    "tsup": "^8.5.1"
  },
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "description": "",
  "name": "light-protocol-stateless-js-kit",
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build": "tsup",
    "check-types": "tsc -b --noEmit",
    "codama:generate": "bun x codama run --all && bun lint:fix",
    "ci": "bun run build && bun run lint && bun run check-types && bun run test",
    "lint": "biome check --error-on-warnings",
    "lint:fix": "biome check --error-on-warnings --write",
    "postinstall": "[ -f .env.example ] && [ ! -f .env ] && cp -v .env.example .env || true",
    "ruler:apply": "ruler apply --local-only",
    "test": "bun test test/index.test.ts",
    "test:e2e": "bun test test/e2e.test.ts",
    "test:watch": "bun test --watch"
  },
  "type": "module",
  "types": "./dist/index.d.ts",
  "version": "0.0.0"
}
