/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  type Codec,
  combineCodec,
  type Decoder,
  type Encoder,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  transformEncoder,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit'
import { LIGHT_SYSTEM_PROGRAM_PROGRAM_ADDRESS } from '../programs/index.ts'
import { getAccountMetaFactory, type ResolvedAccount } from '../shared/index.ts'

export const INVOKE_DISCRIMINATOR = new Uint8Array([26, 16, 169, 7, 21, 202, 242, 25])

export function getInvokeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INVOKE_DISCRIMINATOR)
}

export type InvokeInstruction<
  TProgram extends string = typeof LIGHT_SYSTEM_PROGRAM_PROGRAM_ADDRESS,
  TAccountFeePayer extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountRegisteredProgramPda extends string | AccountMeta<string> = string,
  TAccountNoopProgram extends string | AccountMeta<string> = string,
  TAccountAccountCompressionAuthority extends string | AccountMeta<string> = string,
  TAccountAccountCompressionProgram extends string | AccountMeta<string> = string,
  TAccountSolPoolPda extends string | AccountMeta<string> = string,
  TAccountDecompressionRecipient extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFeePayer extends string
        ? WritableSignerAccount<TAccountFeePayer> & AccountSignerMeta<TAccountFeePayer>
        : TAccountFeePayer,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountRegisteredProgramPda extends string
        ? ReadonlyAccount<TAccountRegisteredProgramPda>
        : TAccountRegisteredProgramPda,
      TAccountNoopProgram extends string ? ReadonlyAccount<TAccountNoopProgram> : TAccountNoopProgram,
      TAccountAccountCompressionAuthority extends string
        ? ReadonlyAccount<TAccountAccountCompressionAuthority>
        : TAccountAccountCompressionAuthority,
      TAccountAccountCompressionProgram extends string
        ? ReadonlyAccount<TAccountAccountCompressionProgram>
        : TAccountAccountCompressionProgram,
      TAccountSolPoolPda extends string ? WritableAccount<TAccountSolPoolPda> : TAccountSolPoolPda,
      TAccountDecompressionRecipient extends string
        ? WritableAccount<TAccountDecompressionRecipient>
        : TAccountDecompressionRecipient,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >

export type InvokeInstructionData = {
  discriminator: ReadonlyUint8Array
  inputs: ReadonlyUint8Array
}

export type InvokeInstructionDataArgs = { inputs: ReadonlyUint8Array }

export function getInvokeInstructionDataEncoder(): Encoder<InvokeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['inputs', addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: INVOKE_DISCRIMINATOR }),
  )
}

export function getInvokeInstructionDataDecoder(): Decoder<InvokeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['inputs', addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ])
}

export function getInvokeInstructionDataCodec(): Codec<InvokeInstructionDataArgs, InvokeInstructionData> {
  return combineCodec(getInvokeInstructionDataEncoder(), getInvokeInstructionDataDecoder())
}

export type InvokeInput<
  TAccountFeePayer extends string = string,
  TAccountAuthority extends string = string,
  TAccountRegisteredProgramPda extends string = string,
  TAccountNoopProgram extends string = string,
  TAccountAccountCompressionAuthority extends string = string,
  TAccountAccountCompressionProgram extends string = string,
  TAccountSolPoolPda extends string = string,
  TAccountDecompressionRecipient extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Fee payer needs to be mutable to pay rollover and protocol fees. */
  feePayer: TransactionSigner<TAccountFeePayer>
  authority: TransactionSigner<TAccountAuthority>
  registeredProgramPda: Address<TAccountRegisteredProgramPda>
  noopProgram: Address<TAccountNoopProgram>
  /** This pda is used to invoke the account compression program. */
  accountCompressionAuthority: Address<TAccountAccountCompressionAuthority>
  /** Merkle trees. */
  accountCompressionProgram: Address<TAccountAccountCompressionProgram>
  /**
   * Sol pool pda is used to store the native sol that has been compressed.
   * It's only required when compressing or decompressing sol.
   */
  solPoolPda?: Address<TAccountSolPoolPda>
  /**
   * Only needs to be provided for decompression as a recipient for the
   * decompressed sol.
   * Compressed sol originate from authority.
   */
  decompressionRecipient?: Address<TAccountDecompressionRecipient>
  systemProgram?: Address<TAccountSystemProgram>
  inputs: InvokeInstructionDataArgs['inputs']
}

export function getInvokeInstruction<
  TAccountFeePayer extends string,
  TAccountAuthority extends string,
  TAccountRegisteredProgramPda extends string,
  TAccountNoopProgram extends string,
  TAccountAccountCompressionAuthority extends string,
  TAccountAccountCompressionProgram extends string,
  TAccountSolPoolPda extends string,
  TAccountDecompressionRecipient extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LIGHT_SYSTEM_PROGRAM_PROGRAM_ADDRESS,
>(
  input: InvokeInput<
    TAccountFeePayer,
    TAccountAuthority,
    TAccountRegisteredProgramPda,
    TAccountNoopProgram,
    TAccountAccountCompressionAuthority,
    TAccountAccountCompressionProgram,
    TAccountSolPoolPda,
    TAccountDecompressionRecipient,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InvokeInstruction<
  TProgramAddress,
  TAccountFeePayer,
  TAccountAuthority,
  TAccountRegisteredProgramPda,
  TAccountNoopProgram,
  TAccountAccountCompressionAuthority,
  TAccountAccountCompressionProgram,
  TAccountSolPoolPda,
  TAccountDecompressionRecipient,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LIGHT_SYSTEM_PROGRAM_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    accountCompressionAuthority: {
      isWritable: false,
      value: input.accountCompressionAuthority ?? null,
    },
    accountCompressionProgram: {
      isWritable: false,
      value: input.accountCompressionProgram ?? null,
    },
    authority: { isWritable: false, value: input.authority ?? null },
    decompressionRecipient: {
      isWritable: true,
      value: input.decompressionRecipient ?? null,
    },
    feePayer: { isWritable: true, value: input.feePayer ?? null },
    noopProgram: { isWritable: false, value: input.noopProgram ?? null },
    registeredProgramPda: {
      isWritable: false,
      value: input.registeredProgramPda ?? null,
    },
    solPoolPda: { isWritable: true, value: input.solPoolPda ?? null },
    systemProgram: { isWritable: false, value: input.systemProgram ?? null },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  // Original args.
  const args = { ...input }

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.feePayer),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.registeredProgramPda),
      getAccountMeta(accounts.noopProgram),
      getAccountMeta(accounts.accountCompressionAuthority),
      getAccountMeta(accounts.accountCompressionProgram),
      getAccountMeta(accounts.solPoolPda),
      getAccountMeta(accounts.decompressionRecipient),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInvokeInstructionDataEncoder().encode(args as InvokeInstructionDataArgs),
    programAddress,
  } as InvokeInstruction<
    TProgramAddress,
    TAccountFeePayer,
    TAccountAuthority,
    TAccountRegisteredProgramPda,
    TAccountNoopProgram,
    TAccountAccountCompressionAuthority,
    TAccountAccountCompressionProgram,
    TAccountSolPoolPda,
    TAccountDecompressionRecipient,
    TAccountSystemProgram
  >)
}

export type ParsedInvokeInstruction<
  TProgram extends string = typeof LIGHT_SYSTEM_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    /** Fee payer needs to be mutable to pay rollover and protocol fees. */
    feePayer: TAccountMetas[0]
    authority: TAccountMetas[1]
    registeredProgramPda: TAccountMetas[2]
    noopProgram: TAccountMetas[3]
    /** This pda is used to invoke the account compression program. */
    accountCompressionAuthority: TAccountMetas[4]
    /** Merkle trees. */
    accountCompressionProgram: TAccountMetas[5]
    /**
     * Sol pool pda is used to store the native sol that has been compressed.
     * It's only required when compressing or decompressing sol.
     */
    solPoolPda?: TAccountMetas[6] | undefined
    /**
     * Only needs to be provided for decompression as a recipient for the
     * decompressed sol.
     * Compressed sol originate from authority.
     */
    decompressionRecipient?: TAccountMetas[7] | undefined
    systemProgram: TAccountMetas[8]
  }
  data: InvokeInstructionData
}

export function parseInvokeInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedInvokeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    // TODO: Coded error.
    throw new Error('Not enough accounts')
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount()
    return accountMeta.address === LIGHT_SYSTEM_PROGRAM_PROGRAM_ADDRESS ? undefined : accountMeta
  }
  return {
    accounts: {
      accountCompressionAuthority: getNextAccount(),
      accountCompressionProgram: getNextAccount(),
      authority: getNextAccount(),
      decompressionRecipient: getNextOptionalAccount(),
      feePayer: getNextAccount(),
      noopProgram: getNextAccount(),
      registeredProgramPda: getNextAccount(),
      solPoolPda: getNextOptionalAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInvokeInstructionDataDecoder().decode(instruction.data),
    programAddress: instruction.programAddress,
  }
}
